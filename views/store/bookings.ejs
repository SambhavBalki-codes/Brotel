<%- include("../partials/head", {pageTitle: "Bookings", isLoggedIn}) %>
</head>

  <%- include("../partials/header",{user : user}) %>

  <% if(isLoggedIn) { %>
  <main class="min-h-screen flex items-center justify-center px-4">
    <div class="bg-white p-8 rounded-2xl shadow-card border border-surface-200 w-96 text-center">
      <span class="inline-block mb-3 px-3 py-1 rounded-full bg-primary-50 text-primary-700 text-xs font-semibold uppercase tracking-wide">
        Your Trips
      </span>
      <h1 class="text-2xl font-bold text-gray-900">Bookings</h1>
    </div>
  </main>
  <% }%>

</body>
</html>